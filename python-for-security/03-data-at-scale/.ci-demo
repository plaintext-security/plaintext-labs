Validated: `make up && make demo && make down` green — streams the real Suricata eve.json replayed 200× (~175k events, flat memory), duckdb columnar triage (top STRRAT signature / loudest-talker dest_ip / per-hour volume), structlog JSON. Offline, no keys.
