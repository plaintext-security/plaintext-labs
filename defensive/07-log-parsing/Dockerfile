# Reference lab image for Module 07 — Log Parsing & Normalisation.
# The worked-example harness is stdlib Python; the lab has you rebuild the same
# normalisation in Vector (VRL), the production tool.
FROM python:3.12-slim
WORKDIR /lab
COPY . /lab
CMD ["bash"]
