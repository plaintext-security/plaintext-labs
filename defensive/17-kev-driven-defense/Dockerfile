# Reference lab image for Module 17 — KEV-Driven Defense.
# Pure stdlib Python: the KEV refresh tool and the detection matcher need no
# third-party packages, which keeps the demo deterministic and offline-safe.
FROM python:3.12-slim

WORKDIR /lab
COPY . /lab

CMD ["bash"]
