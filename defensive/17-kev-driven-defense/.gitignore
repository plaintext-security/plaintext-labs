# The exploit target is pulled on demand by `make target-up`, never committed.
target/
# Learner-generated lab artifacts stay out of the repo.
*.pcap
*.log.gz
# The bundled sample access log IS committed seed data — re-include it despite the
# repo-wide `*.log` ignore (the demo's detection step reads it; it must ship).
!data/solr_access.log
