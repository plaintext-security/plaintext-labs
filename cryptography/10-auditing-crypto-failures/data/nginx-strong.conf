server {
    listen 443 ssl;
    server_name nginx-strong;

    ssl_certificate     /etc/nginx/certs/server.crt;
    ssl_certificate_key /etc/nginx/certs/server.key;

    # STRONG: TLS 1.3 only (Mozilla modern profile)
    ssl_protocols TLSv1.3;

    # STRONG: TLS 1.3 AEAD cipher suites only. TLS 1.3 suites are selected via OpenSSL's
    # Ciphersuites command, NOT ssl_ciphers (which only governs TLS 1.2 and below).
    ssl_conf_command Ciphersuites TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:TLS_AES_128_GCM_SHA256;
    ssl_prefer_server_ciphers off;

    # HSTS: 2 years + preload
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

    # Security headers
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options DENY always;
    add_header X-XSS-Protection "1; mode=block" always;

    location / {
        return 200 "New microservice endpoint (TLS 1.3, strong config)\n";
        add_header Content-Type text/plain;
    }
}
