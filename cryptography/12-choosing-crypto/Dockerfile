# Module 12 — Choosing Your Crypto (ADR linter).
# This is a writing/decision lab; the only runnable piece is adr-lint.py, which
# is dependency-free Python 3. A tiny pinned slim image is all it needs.
FROM python:3.12-slim

WORKDIR /lab
COPY . /lab

CMD ["bash"]
