server {
    listen 443 ssl;
    server_name nginx-strong;

    ssl_certificate     /etc/nginx/certs/server.crt;
    ssl_certificate_key /etc/nginx/certs/server.key;

    # STRONG: TLS 1.3 only (Mozilla "modern" profile)
    ssl_protocols TLSv1.3;

    # STRONG: TLS 1.3 AEAD cipher suites only
    ssl_ciphers TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:TLS_AES_128_GCM_SHA256;

    # Client selects preferred cipher (TLS 1.3 default)
    ssl_prefer_server_ciphers off;

    # HSTS: 2 years, all subdomains
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

    # Security headers
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options DENY always;

    location / {
        return 200 "Corp API (strong TLS config)\n";
        add_header Content-Type text/plain;
    }
}
