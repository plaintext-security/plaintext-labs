make demo validated green on Linux (pwsh 7.4, PSScriptAnalyzer 1.22.0, Pester 5.6.1, SecretManagement 1.1.2, SecretStore 1.0.6): analyzer clean over ./Vigil, 14 passed / 1 skipped (Test-PSSessionConfigurationFile is Windows-only), secret round-trip runs live, JEA .psrc/.pssc well-formed and read-only, gate exits 0. JEA enforcement + WinRM remoting are Windows-only (optional VM step).
