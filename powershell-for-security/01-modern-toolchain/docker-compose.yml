services:
  lab:
    build: .
    image: plaintext/powershell-modern-toolchain:1.0
    working_dir: /lab
    volumes:
      - ./:/lab
    command: pwsh -NoLogo -Command "Start-Sleep -Seconds 2147483647"
