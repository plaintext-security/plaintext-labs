# Meridian Payment Processor

Internal payment processing service.

## Configuration

All configuration is via environment variables. See `config.py` for the full list.
Never hardcode credentials — use the IAM role attached to the service or retrieve from Vault.
