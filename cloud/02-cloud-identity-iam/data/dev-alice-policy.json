{
  "_comment": "The original over-broad policy attached to dev-alice (MeridianDevPolicy). The PassAnyRole statement is the escalation: iam:PassRole on * lets dev-alice pass MeridianEC2AdminRole (AdministratorAccess) to a new EC2 instance. Run: python check_escalation.py data/dev-alice-policy.json",
  "Version": "2012-10-17",
  "Statement": [
    {"Sid": "S3All", "Effect": "Allow", "Action": "s3:*", "Resource": "*"},
    {"Sid": "EC2RunInstances", "Effect": "Allow",
     "Action": ["ec2:RunInstances", "ec2:DescribeInstances", "ec2:TerminateInstances"], "Resource": "*"},
    {"Sid": "PassAnyRole", "Effect": "Allow", "Action": "iam:PassRole", "Resource": "*"},
    {"Sid": "ListIAM", "Effect": "Allow", "Action": ["iam:List*", "iam:Get*"], "Resource": "*"}
  ]
}
