services:
  lab:
    build: .
    image: plaintext/memory-corruption-lab
    working_dir: /lab
    volumes:
      - ./:/lab
    command: sleep infinity
    security_opt:
      - seccomp:unconfined   # gdb requires ptrace; unconfined enables it in demo
